const SectionTitle = ({ text }) => {
  return (
    <article className="border-b-2 border-gray-300 pb-6 ">
      <h2 className="text-3xl font-bold capitalize tracking-wide">{text}</h2>
    </article>
  );
};

export default SectionTitle;
